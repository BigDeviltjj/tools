PROJECT(glogDemo)
CMAKE_MINIMUM_REQUIRED ( VERSION 2.8.4 )
FIND_PACKAGE(glog REQUIRED)
INCLUDE_DIRECTORIES(${glog_INCLUDE_DIR})
ADD_EXECUTABLE(useglog main.cpp)
TARGET_LINK_LIBRARIES(useglog ${glog_LIBRARIES})
